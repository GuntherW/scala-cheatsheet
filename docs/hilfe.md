###  Automatisches setzen der Javaversion pro Projekt mit sdkman

1. In das Verzeichnis wechseln
2. sdk env init (`.sdkmanrc` wird angelegt)
3. In der `~/.sdkman/etc/config` den Wert `sdkman_auto_env=true` setzen.
